## What is the `MCP`?

## How does it works?

## Getting started with `MCP-Servers`

There some sdk's... (include official python sdk)

We will use FastMCP - based on starlette framework for creation an mcp-servers.

## Why should use `MCP` instead of simple functions?
